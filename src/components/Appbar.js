import React from 'react';

const Appbar = () => {
    return (
        <div className="appbar">
            <h1>TodoPelota</h1>
            <button>Login</button>
        </div>
    );
};

export default Appbar;